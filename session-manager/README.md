# session-manager

Session and slot ownership layer.

Planned ownership:
- session persistence boundaries
- slot assignment state
- controller profile assignment state
- install-root and session-path defaults
